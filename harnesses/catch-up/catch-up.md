---
description: Orient in the current repo — summarize recent history and uncommitted work
---

Give me a quick orientation on the current repository so I can pick up where I left off:

1. Current branch and how it differs from the default branch (`git status`, `git log --oneline` of recent commits).
2. Any uncommitted or staged changes, summarized by intent (not a raw diff dump).
3. Any stashes.
4. A one-paragraph "here's where things stand" summary and a suggested next step.

Keep it tight — this is a glance, not a deep review.
